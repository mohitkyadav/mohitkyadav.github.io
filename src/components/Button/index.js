import React from 'react'

import './index.scss'

const Button = () => (
  <div className="btn-container">
    <button
      className="btn-container__btn"
      type="button"
    >
      Click me!
    </button>
  </div>
)

export default Button
